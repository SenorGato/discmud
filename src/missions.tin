#CLASS {missions} {kill};
#CLASS {missions} {open};

#script {old_jobs} {#system psql -U postgres -c "SELECT * from discmud.timers WHERE failure IS NULL;"}

#FOREACH {old_jobs} {job} {#showme $job;}
#NOP In FOREACH test each job(-2 init) for time, if >4 hours write fail else add to current_jobs

#ALIAS {miss} {offer to deliver %1; #var {job_type} {%1}}
#ACTION {^Frank.*$} {#SHOWME "We matched frank"}
#CLASS {missions} {close};
